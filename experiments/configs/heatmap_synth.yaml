# Parallel baseline: polar heatmap + peak extraction (same SRP input).
_base_: base.yaml
exp_name: heatmap_synth
data:
  features: [srp]
model:
  name: heatmap
  max_peaks: 12
  target_sigma_bins: 1.0
  channels: [32, 64, 64]
